import 'package:flutter/material.dart';

class PastOrderPage extends StatelessWidget {
  const PastOrderPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("PastOrderPage"),
    );
  }
}
