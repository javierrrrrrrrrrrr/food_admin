import 'package:flutter/material.dart';

class OngoingOrderPage extends StatelessWidget {
  const OngoingOrderPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("OngoingOrderPage"),
    );
  }
}
